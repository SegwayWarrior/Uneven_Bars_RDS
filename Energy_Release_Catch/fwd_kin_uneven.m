%% fwd_kin_uneven.m
%
% Description:
%   Wrapper function for autogen_fk_com.m
%   Computes the locations of the CoMs of the spine and body.
%
% Inputs:
%   x: the state vector, q = [x; y; theta_1; theta_2; theta_3];
%   params: a struct with many elements, generated by calling init_params.m
%
% Outputs:
%   FK = x and z locations of CoM and tips
function [FK] = fwd_kin_uneven(q,params)

x = q(1);
y = q(2);
theta_1 = q(3);
theta_2 = q(4);
theta_3 = q(5);

[FK] = autogen_fwd_kin(params.model.geom.bot.l,...
                       params.model.geom.mid.l,...
                       params.model.geom.top.l,...
                       params.model.dyn.mid.r_com,...
                       params.model.dyn.bot.r_com,...
                       params.model.dyn.top.r_com,...
                       theta_1,...
                       theta_2,...
                       theta_3,...
                       x,...
                       y);

end
